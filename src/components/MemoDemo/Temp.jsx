import {memo} from 'react'

export const Temp = () => {
  


  return (

    <div>This is component completely unrelated to the any other component in the app</div> 
  )
}

export default memo(Temp);
